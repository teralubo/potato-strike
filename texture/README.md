# Local FreEDoom source

Place `freedoom2.wad` or `freedoom1.wad` in this directory and run:

```text
npm run assets:freedoom
```

The WAD files are ignored by Git and are not required at runtime. The extractor writes the small, standalone weapon PNG files used by Potato Strike to `game/assets/weapons/freedoom/`.
