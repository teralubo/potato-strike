# fps_info

Default Potato Strike performance overlay.

- Category: `default`
- Default state: disabled
- Toggle: `Menu > Mody > default > fps_info`
- Output: a small FPS value in the upper-left corner while a match is running

Edit `mod.json` to change the initial state for new profiles. Existing profiles keep their own GUI setting.
